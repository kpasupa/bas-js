node_modules/
*.log
__pycache__/
.DS_Store

# Local scratch / test artifacts
_*.html

# Data + program files live in the picked folder, never in the app repo (public)
*.DAT
*.BAS
CUSTOMER
cheque/

# Allow sample folder in repo (incl. nested menu/demo folders)
!sample/
!sample/**/*.BAS

# Browser profile — Chrome runtime data, not source
browser-profile/
